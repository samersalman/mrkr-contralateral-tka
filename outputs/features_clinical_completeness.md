# Clinical feature table — completeness and imputation report

Generated by `src/features_clinical.py` (Phase 2, Track A). Aggregate only: this file contains no patient identifiers.

## Cohort

- Patients (one row per `empi_anon`): **3,709**
- 5-year contralateral-TKA events: **533** (14.37%)
- Splits are the LOCKED assignment from `src/splits.py`; the **test split is sealed** and is present here only so the frozen transform can be applied to it.

| split | n patients | n events | event rate % |
|---|---|---|---|
| train | 2,597 | 373 | 14.36 |
| val | 371 | 54 | 14.56 |
| test | 741 | 106 | 14.30 |

## Predictor set (protocol Table 7 model ladder, protocol section 21)

Protocol Table 7 defines **M0 = "Age, sex, comorbidities, pain, image-to-index interval"** and **M1 = "M0 plus inferred KLG"**, and protocol Table 6 lists the dataset-inferred contralateral KLG as a **secondary comparator only**. The two column lists are frozen separately in `derived-data/cohort/clinical_imputation_params.json` (`model_columns` and `m1_model_columns`).

**M0 — primary clinical comparator (11 predictors)**, model-ready as `<name>_imp`:

- `age_at_index`
- `sex_female`
- `days_to_index`
- `knee_pain_any`
- `pain_score_max`
- `obesity`
- `diabetes`
- `hypertension`
- `autoimmune`
- `nicotine_use`
- `trauma_lower_extremity`

**M1 — secondary severity comparator (12 predictors)** = M0 plus `klg_contra`.

- Fitted on the **KLG-eligible subset only** (klg_contra observed (not NaN) — i.e. klg_contra_missing == 0): **3,566** of 3,709 patients (96.1%); 143 patients have no inferred contralateral KLG.
- Eligible by split: train 2,500 (351 events), val 359 (52 events), test 707 (98 events).
- `klg_contra` is **never median-imputed for M1** — imputing a radiograph-derived severity grade for the patients who have no eligible bilateral frontal image would invent the very measurement M1 exists to test. The `_imp` column is written for uniformity, but on the eligible subset it equals the observed value on every row.

Held out of the M0 predictor set on purpose:

- `klg_contra` — dataset-inferred contralateral KLG. Protocol Table 6: **secondary comparator only**; protocol Table 7 puts it in **M1**. Keeping it in M0 would put a radiograph-derived severity grade inside the very comparator the image models must beat, which biases the study's primary estimand (does imaging improve prediction beyond routine clinical variables) toward zero.
- `race`, `race_major` — retained for subgroup / fairness EVALUATION only (`race_in_primary_predictors: False`).
- `knee_pain_contra` — side-specific pain. `pain.laterality` is coded on only ~5% of source rows, so this variable is largely unobserved (3,362 of 3,709 patients have any laterality-coded pre-index pain row, flagged by `knee_pain_contra_observed`); a 0 mostly means 'not coded', not 'no contralateral pain'. Descriptive use only.

## Carried evaluation-only columns (protocol sections 21 and 24)

Copied verbatim from `final_cohort.parquet` (the same source `src/subgroups.py` reads; it agrees with `selected_studies.parquet` on all 3,709 patients, so no re-join is done). These columns are **never predictors** — the module asserts each one out of `primary_predictors` and out of `model_columns`. They are here so the section-21 equity/performance audit can report a **weight-bearing-status subgroup** and the section-24 **"weight-bearing frontal radiographs only" sensitivity analysis** can be run downstream.

| column | dtype | n missing | distribution |
|---|---|---|---|
| `weight_bearing_frontal` | bool | 0 | False = 372 (10.0%), True = 3,337 (90.0%) |

## Contralateral KLG selection rule

Frontal (`view_position == 'F'`) images of the patient's SELECTED pre-index study only. (1) keep the frontal(s) with the smallest `days_to_index`; (2) read `L_KLG_inference` when `contra_side == 'L'`, else `R_KLG_inference`; (3) average over any remaining tie (order-independent, deterministic), skipping NaN.

- eligible frontal images: **4,269** across **3,690** patients
- frontals per patient: 1: 3,121, 2: 560, 3: 8, 4: 1
- patients with no frontal at all (tier `no_frontal`): **19**
- patients resolved by averaging a tie (non-integer KLG): **173**
- `klg_contra` missing after the rule: **143** (3.86%)

## Observation coverage (a 0 is not always an observed 0)

- Patients with **zero** pre-index ICD rows: **0** — every patient contributes at least one pre-index diagnosis row, so every comorbidity 0 in this table is an OBSERVED negative, not an unobserved one.
- Patients with zero pre-index pain rows inside the 365-day lookback: **77** — their `knee_pain_any` 0 is likewise unobserved rather than observed-negative.

## Imputation

**Fit on `split == "train"` rows ONLY (n = 2,597), then applied unchanged to val and test.** No statistic in this table was computed from validation or test data. The frozen fill values live in `derived-data/cohort/clinical_imputation_params.json` and are replayed verbatim by the downstream model modules — they are never refit.

| column | strategy | fill value | n missing (train) | n missing (all) | imputed column | indicator column |
|---|---|---|---|---|---|---|
| `age_at_index` | median | 66.0 | 0 | 0 | `age_at_index_imp` | — |
| `sex_female` | median | 1.0 | 0 | 0 | `sex_female_imp` | — |
| `days_to_index` | median | 98.0 | 0 | 0 | `days_to_index_imp` | — |
| `knee_pain_any` | median | 1.0 | 0 | 0 | `knee_pain_any_imp` | — |
| `pain_score_max` | median | 7.0 | 172 | 268 | `pain_score_max_imp` | `pain_score_max_missing` |
| `obesity` | median | 0.0 | 0 | 0 | `obesity_imp` | — |
| `diabetes` | median | 0.0 | 0 | 0 | `diabetes_imp` | — |
| `hypertension` | median | 1.0 | 0 | 0 | `hypertension_imp` | — |
| `autoimmune` | median | 0.0 | 0 | 0 | `autoimmune_imp` | — |
| `nicotine_use` | median | 0.0 | 0 | 0 | `nicotine_use_imp` | — |
| `trauma_lower_extremity` | median | 0.0 | 0 | 0 | `trauma_lower_extremity_imp` | — |
| `klg_contra` | median | 2.0 | 97 | 143 | `klg_contra_imp` | `klg_contra_missing` |

## Model columns, and the ones that are deliberately not model columns

`model_columns` (the **M0** list `src/model_clinical.py` and the Colab notebook build the design matrix from) holds **11** columns: 11 `<predictor>_imp` columns plus the missing indicators that are *identified* and whose own predictor is in M0.

`m1_model_columns` holds **12** columns — the M0 list plus `klg_contra_imp`.

| column | excluded from `m1_model_columns` because |
|---|---|
| `klg_contra_missing` | identically constant on the 3,566 KLG-eligible patients (M1 is fitted only where klg_contra is observed), so it carries no information and has no hazard ratio |

| column | excluded from `model_columns` because |
|---|---|
| `pain_score_max_missing` | exactly aliased with 'knee_pain_any_imp' on all 3,709 rows; a Cox model has no intercept, so the pair spans an unidentified constant and only their difference is estimable. 'knee_pain_any_imp' carries it. The column is still written to the parquet for auditing and for the complete-case sensitivity filter. |
| `klg_contra_imp` | M1-only column: protocol Table 6 lists dataset-inferred KLG as a SECONDARY comparator and Table 7 puts it in M1 ('M0 plus inferred KLG'), so it is out of the M0 design. It is written to the parquet and listed in `m1_model_columns`. |
| `klg_contra_missing` | M1-only column: protocol Table 6 lists dataset-inferred KLG as a SECONDARY comparator and Table 7 puts it in M1 ('M0 plus inferred KLG'), so it is out of the M0 design. It is written to the parquet and listed in `m1_model_columns`. |

**Why this matters, stated once.** `pain_score_max` is aggregated only over rows with `knee_pain == '1'`: protocol Table 5 defines the predictor as a *knee* pain score, and scoring it over every pre-index pain row would admit shoulder and back scores. The arithmetic consequence in this cohort is exact — a pain score exists for **precisely** the patients with a pre-index knee-pain record (3,441 of 3,709) — so *no pre-index knee-pain record* and *no pain score* are the **same event** and `knee_pain_any_imp + pain_score_max_missing = 1` on every row. A Cox partial likelihood has no intercept, so that pair spans a direction the likelihood cannot see: ridge still fits, but it splits one shared effect into two equal and opposite coefficients, neither interpretable alone. Dropping the indicator loses no information — `knee_pain_any_imp` **is** the absence indicator, and its coefficient is now the identified quantity. The column is still written to the parquet, and the complete-case sensitivity analysis still filters on it. The identity is re-verified on every run rather than assumed, so a cohort in which the two stop coinciding will automatically re-admit the indicator.

## Per-column missingness (raw, before imputation)

| column | kind | primary | n missing (all) | % missing | train | val | test |
|---|---|---|---|---|---|---|---|
| `age_at_index` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `sex_female` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `days_to_index` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `knee_pain_any` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `pain_score_max` | predictor | yes | 268 | 7.23 | 172 | 36 | 60 |
| `obesity` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `diabetes` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `hypertension` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `autoimmune` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `nicotine_use` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `trauma_lower_extremity` | predictor | yes | 0 | 0.00 | 0 | 0 | 0 |
| `klg_contra` | predictor |  | 143 | 3.86 | 97 | 12 | 34 |
| `klg_contra_missing` | missing_indicator |  | 0 | 0.00 | 0 | 0 | 0 |
| `pain_score_max_missing` | missing_indicator |  | 0 | 0.00 | 0 | 0 | 0 |
| `age_at_index_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `sex_female_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `days_to_index_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `knee_pain_any_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `pain_score_max_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `obesity_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `diabetes_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `hypertension_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `autoimmune_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `nicotine_use_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `trauma_lower_extremity_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `klg_contra_imp` | imputed |  | 0 | 0.00 | 0 | 0 | 0 |
| `sex` | eval_only |  | 0 | 0.00 | 0 | 0 | 0 |
| `race` | eval_only |  | 0 | 0.00 | 0 | 0 | 0 |
| `race_major` | eval_only |  | 0 | 0.00 | 0 | 0 | 0 |
| `knee_pain_contra` | eval_only |  | 0 | 0.00 | 0 | 0 | 0 |
| `knee_pain_contra_observed` | eval_only |  | 0 | 0.00 | 0 | 0 | 0 |
| `weight_bearing_frontal` | eval_only_carried |  | 0 | 0.00 | 0 | 0 | 0 |
| `n_icd_preindex_rows` | audit |  | 0 | 0.00 | 0 | 0 | 0 |
| `n_pain_preindex_rows` | audit |  | 0 | 0.00 | 0 | 0 | 0 |
| `klg_n_frontal` | audit |  | 0 | 0.00 | 0 | 0 | 0 |
| `event_indicator` | label |  | 0 | 0.00 | 0 | 0 | 0 |
| `time_from_landmark` | label |  | 0 | 0.00 | 0 | 0 | 0 |
| `complete_5y` | label |  | 0 | 0.00 | 0 | 0 | 0 |
| `censor_reason` | label |  | 0 | 0.00 | 0 | 0 | 0 |

Every `*_imp` column has zero missingness by construction (asserted in code). The full long-format table is `outputs/tables/features_clinical_completeness.csv`.

