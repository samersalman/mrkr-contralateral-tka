# Crop QA sign-off — contralateral-knee crops

**STATUS (2026-08-12): UNSIGNED, and it will stay unsigned.** This file was written as a pre-training gate and its original banner read "TRAINING IS BLOCKED UNTIL THIS FILE IS SIGNED". **That is no longer what happened.** The seven-arm ladder was trained on 2026-07-29 and the locked test split was read once on the same day, with this block still empty, on an explicit author instruction recorded in deviation **D28** and **D31**. The gate that would have refused lived in `notebooks/train_colab.ipynb`, which was superseded by `notebooks/train_and_score_colab.ipynb`; the replacement audits checkpoints, not this checklist.

**The three reviews this file covers are now in three different states, and section 6 is the record of what each one is.** The protocol **section 7** laterality audit (section 5 below) is **DONE**: 98.0% agreement on 200 development patients, adjudicated on paper. The protocol **section 23 (i)** 400-image two-reviewer image review (section 3 below) is **DECLINED** by author decision, registered as deviation **D40**; it will not be performed, and no part of this file may be read as a queue of pending work for it. The protocol **section 23 (ii)** 200-record outcome audit (section 4 below) is **sampled and unadjudicated**, and is the one item still outstanding. Deviation **D22** is **PARTIAL** and states all three plainly. **This file must never be signed.** Signing asserts, in its own words, that "the protocol section 23 audits above are adjudicated and passed"; one of them is declined and the other is unadjudicated, so no signature can be truthful.

**READ THIS BEFORE RE-RUNNING ANYTHING (updated 2026-08-12).** This file was originally generated by `python3 -m src.crop_qa`. **Do not run that command to refresh it, and do not run it at all.** It rebuilds the contact sheet and all three audit workbooks from a DICOM tree that no longer exists on this machine; it would overwrite the hand-corrected split labelling, the train+val preprocessing record in section 2 and every 2026-08-12 status statement in this file with whatever a partial run could reconstruct; and it would re-create the sampling material that was deliberately removed on 2026-08-12, which would misrepresent a declined review (**D40**) as work in progress. **The file is now hand-maintained.** **It is also load-bearing** — see the note at the end of section 2, and the note in section 6 on which of its clauses the manuscript reads.

**Why crop fidelity is the non-negotiable.** **4,075 of the 4,269 frontal films in this cohort are bilateral** — at **manifest** level, across all three splits — both knees on one image — so the correct knee is selected from metadata (`laterality == 'B'`, `contra_side`, `horizontal_flip`, and the radiological display convention). An inverted sign there would train the model on the knee that has ALREADY been replaced, and every downstream metric would be meaningless. No automated test can close that gap; a human must look at the images.

**The leading clause of that paragraph is machine-read. Do not reword it.** `src.make_manuscript.manual_review_clause` scrapes it with the regex `([\d,]+)\s+of\s+the\s+([\d,]+)\s+frontal films in this cohort are bilateral` and prints both captures inside a Discussion limitation; `tests/test_make_manuscript.py` asserts the round trip. **This has already broken once**: an edit that inserted a parenthetical between "cohort" and "are bilateral" stopped the match, both counts vanished from the manuscript, and the only symptom was a word-budget test failing three steps downstream. Qualifiers belong *after* "bilateral", as the em-dashes above put them, never inside the matched clause.

`horizontal_flip` is a MODEL-INFERRED MRKR annotation with an unquantified error rate and no DICOM tag to check it against. **It drives the half-select on 287 finished crops across all three splits (train 195 / val 29 / test 63.)** That is the specific thing panel A of the contact sheet exists to let you verify — and, for the 63 test-split images, the specific thing that can no longer be verified at all (section 3).

## 1. Reviewer criteria — tick every box

How to read a contact-sheet row: the **left panel** is the full film, flip-corrected and NOT mirrored, with the half the pipeline KEPT outlined in green and the discarded half marked INDEX in red. The **right panel** is that crop before the left/right mirror. Judge criterion 1 on the LEFT panel; criteria 2 and 3 on the RIGHT panel.

- [ ] the outlined half of the full film is the CONTRALATERAL knee (opposite index_side)
- [ ] the crop contains no pixels from the index knee or the midline
- [ ] no residual burned-in laterality marker or text
- [ ] the sample above is representative (all view x side cells populated)

**These boxes are machine-counted. Keep the `- [ ]` markers exactly as they are, and do not tick a box for a review that has not happened.** `src.make_manuscript.manual_review_clause` counts the unticked ones with the regex `^\s*-\s*\[\s\]` and states that count in the Discussion's limitations; `tests/test_make_manuscript.py` fails if none is left unticked. Ticking a box, or restyling the list, removes a disclosure from the manuscript silently. **As of 2026-08-12 these four boxes will never be ticked**: the review that would have ticked them is declined (**D40**). They stay here, unticked, as the standing record that nobody looked — not as a to-do list.

**Criterion 1 is undecidable on the 82 test-split rows of the section-23(i) sample**, which have no full film. See section 3.

## 2. Evidence

**Every statistic in this section names the splits it covers. Nothing here is a whole-cohort number unless it says so.**

- Contact sheet: `outputs/figures/crop_qa_contact_sheet.png` — **train and val only.**
- Splits sampled for the contact sheet: **train, val**. The test split was still sealed when the sheet was rendered on 2026-07-26, and the test crops did not exist until 2026-07-29. **The sheet can never be extended to the test split: the source DICOMs are gone, so no further full-film panel can be rendered.**
- Target per cell: **12**; crop panels rendered: **72**, full-film panels rendered: **72**, across **6** (view x contra_side) cells, all train+val.
- Full films were re-read from the DICOMs for 72 of 72 sampled rows, so half-select is visually decidable **for those 72 train+val rows**.
- Shards inspected at the time of writing: `~/mrkr-shards` (train+val). **That path no longer exists on this machine**; the shards and their `labels.csv` sidecars live on the project's Google Drive, and the finished-crop table below is `derived-data/cohort/preprocess_labels.csv`, which holds all three splits.

### Sample composition — contact sheet tiles (train + val only)

| view | contra_side | tiles | fallback crops | mean crop_confidence | mean masked_pct |
|---|---|---|---|---|---|
| frontal | L | 12 | 0 | 1.000 | 22.8% |
| frontal | R | 12 | 0 | 1.000 | 22.8% |
| lateral | L | 12 | 0 | 1.000 | 23.0% |
| lateral | R | 12 | 0 | 0.988 | 23.4% |
| sunrise | L | 12 | 0 | 1.000 | 23.1% |
| sunrise | R | 12 | 0 | 1.000 | 23.6% |

### Whole-run crop statistics — ALL THREE SPLITS

Two preprocessing runs wrote these crops: **train + val on 2026-07-26**, and **test on 2026-07-29**, in a second deliberate invocation (`--splits test --include-test`). The originally generated version of this section covered the first run only and was silently missing 1,216 test crops. Recomputed on 2026-08-10 directly from `derived-data/cohort/preprocess_labels.csv` (6,071 rows).

| statistic | train + val | test | all splits |
|---|---:|---:|---:|
| crops written | **4,855** | **1,216** | **6,071** |
| patients | 2,966 | 740 | **3,706** |
| crops by split | train 4,254 / val 601 | test 1,216 | 4,254 / 601 / 1,216 |
| frontal / lateral / sunrise | 3,401 / 1,330 / 124 | 858 / 321 / 37 | **4,259 / 1,651 / 161** |
| bilateral (`laterality == 'B'`, all frontal) | 3,255 | 814 | **4,069** |
| single-knee films (needed no half-select) | 1,600 | 402 | **2,002** |
| `crop_method` = `center_default` | 4,786 | 1,199 | **5,985** |
| `crop_method` = `intensity_profile` (localizer moved the box) | 69 | 17 | **86** |
| `crop_method` = `fallback_center` | 0 | 0 | **0** |
| mean `crop_confidence` | 0.9957 | 0.9956 | **0.9957** |
| `masked_pct` mean / p90 / max | 23.10% / 23.64% / 44.75% | 23.06% / 23.65% / 40.89% | **23.09% / 23.64% / 44.75%** |
| crops with padding beyond the border band | 1,248 | 324 | **1,572** |
| `horizontal_flip == 1` | 224 | 63 | **287** |
| `inverted == 1` | 1,196 | 270 | **1,466** |
| orientation after standardization | left 4,855 | left 1,216 | **left 6,071** |
| mirrored to a common laterality | 2,273 | 582 | **2,855** |

- **Half-select applied to 4,069 bilateral frontals; 2,002 single-knee films needed none — all splits.** The train+val-only version of this sentence, which the original file carried without saying so, is 3,255 and 1,600. Every bilateral film in the finished table is a frontal, in all three splits, so "bilateral frontals" is exact rather than shorthand.
- **Crop centre: `localizer_mode` = `center_default`, all splits.** Fallback-localization rate among WRITTEN crops is 0.00% in both runs — 0 of 4,855 train+val (recorded in the prose of this file, which is now the only surviving record of that run) and 0 of 1,216 test (`fallback_rate: 0.0` in `derived-data/cohort/preprocess_run_test.json`). Under `center_default` the deterministic CENTRED box is the primary estimate and the intensity-profile localizer may only move it when it clears `localizer_refine_min_confidence`; it did so on 86 of 6,071 crops. On 800 real TRAIN films that localizer fell back on 26.0% of images (41.4% of single-knee views) and its *successes* were frequently worse than the centre — it locks onto a bright shaft or collimator edge — so a centred crop is a deliberate choice here, NOT a failure, and nothing is excluded for using one.
- **Masked pixels (protocol section 13), all splits: mean 23.09%, p90 23.64%, max 44.75%** (fixed border band 22.75% + out-of-bounds padding; cap `max_masked_pct` = 45%). 1,572 crops carry padding beyond the border band, of which 1,248 are train+val and 324 are test.
- **Burned-in markers surviving into the finished crops: 18.5% of the 1,216 sampled crops — that is ALL 1,216 TEST crops, the complete sealed split rather than a sample of it.** By view: frontal 9.2%, lateral 43.0%, sunrise 21.6%. Measured on the FINISHED crops, so it is what SURVIVED, not what the masker believes it removed. This is an UPPER BOUND — saturated bone edges share the signature — so read it as a list of crops to LOOK AT. Criterion 3 above is the finding of record.
  - **This sentence is machine-read. Do not reword its leading clause.** `src.make_manuscript.manual_review_clause` scrapes it with the regex `Burned-in markers surviving into the finished crops:\s*([\d.]+)% of the ([\d,]+) sampled crops` and prints the two captures in the Discussion's limitations; `tests/test_make_manuscript.py` asserts the round trip and fails if this phrasing changes. **Until 2026-08-11 the scraped line read "16.7% of the 72 sampled crops" and this bullet also asserted that "no test crop was measured".** Both were true when written and are now false: all 1,216 test crops were scanned during the protocol section-22 work on 2026-08-10 (`crop_qa.residual_marker_scan`, recorded in `outputs/tables/interp_regions.csv`). The old line meant the generated manuscript quoted a **train+val** figure inside a limitation about the **reported** split; the figure scraped now is the one covering the images the reported results actually rest on. **The generated sentence still carries no split qualifier of its own — whoever owns `src/make_manuscript.py` should add one.** **Done on 2026-08-11, and it makes the SECOND capture load-bearing too.** `make_manuscript.crop_scan_scope` now names the split in the manuscript by matching that denominator against the per-split crop counts in `derived-data/cohort/preprocess_labels.csv`; it is derived there rather than read from the prose here, so the manuscript cannot inherit a mislabelled split from this file. **Do not put a rounded, partial or re-sampled denominator in this clause** — a count that matches no split or union of splits leaves the manuscript figure unqualified again, which is the conflation the table below exists to prevent. `tests/test_make_manuscript.py::test_the_residual_marker_figure_names_the_split_it_was_measured_on` fails loudly in that case rather than letting it through.
- **THREE marker measurements now exist, on three different samples, and each names its own denominator.** Do not present any one of these three as another.

  | # | Measurement | Sample | Stage | Rate | By view |
  |---|---|---|---|---|---|
  | 1 | marker inside the border band **BEFORE masking** | **500 sampled TRAIN crops** | pre-masking prevalence | **26.4%** | frontal 15.8%, lateral 52.7%, sunrise 50.0% |
  | 2 | surviving marker-like blob in the **finished** crop | **72 sampled crops, TRAIN AND VAL ONLY** | post-masking residual | **16.7%** (mean 0.35 blobs each) | frontal 8.3%, lateral 33.3%, sunrise 8.3% |
  | 3 | surviving marker-like blob in the **finished** crop | **all 1,216 TEST crops** | post-masking residual | **18.5%** | frontal 9.2%, lateral 43.0%, sunrise 21.6% |

  Measurements 2 and 3 are made on the FINISHED crops, so each is what SURVIVED, not what the masker believes it removed. **Both are UPPER BOUNDS** — saturated bone edges share the blob signature — so read either as a list of crops to LOOK AT rather than as a defect rate. Criterion 3 in section 1 above is the finding of record. Measurement 1 is a *prevalence before* masking and is **not** comparable to the other two; measurement 2 and measurement 3 are the same quantity on different splits, and their difference is consistent with the different view mix and with sampling noise at n = 72. Sources: 1 → `src/preprocess_images.py:480`, `config/feasibility.yaml:435–437`; 2 → this file, the 2026-07-26 residual audit of the contact-sheet sample; 3 → `outputs/tables/interp_regions.csv` (`residual_marker_crops_pct`, `residual_marker_by_view_pct`), scanned with `crop_qa.residual_marker_scan`.
- **The masked border band is not exactly zero on every crop, and that is a protocol section 13 contract violation.** It is non-zero on **145 of 1,216 test crops (11.9%)** — frontal 6.8%, lateral 25.2%, sunrise 16.2% — because `preprocess_images.crop_stages` runs `mask_borders` first and `mask_burned_in_markers` second, and the marker masker fills a dilated region with the surrounding ring median, writing values of up to 48/255 back into the band. It touches 0.049% of all border-band pixels. `assert_border_is_zero` never caught it because `TrainingData.__init__` probes exactly one training image. Analytically it is priced at Δ AUROC **−0.0002** by the section-22 occlusion experiment. Recorded as deviation **D39**. `outputs/tables/interp_regions.csv` carries the counts.
- **Protocol section 13 exclusions (never written to a shard):** train+val `excessive_masking` **17**, `localization_failed` **0**; test `excessive_masking` **2**, `localization_failed` **0**. **All splits: 19 images excluded.**
- **All preprocessing failure reasons (counts):** train+val `{'excessive_masking': 17, 'view_mask_drift': 1}`; test `{'excessive_masking': 2, 'view_mask_drift': 1}`. **All splits: 21 failure records covering 19 excluded images** — the two `view_mask_drift` records are reported failures that did not remove an image from a shard.
- **This reconciles exactly, and the reconciliation is worth keeping** because two different bilateral-frontal counts circulate in this project. At **manifest** level, 6,122 selected images become 6,090 kept after dropping 32 'E'/'I' rows, of which frontal 4,269 = **4,075 bilateral + 194 unilateral**, lateral 1,659, sunrise 162, over 3,709 patients (train 4,270 / val 602 / test 1,218). At **finished-crop** level the same quantities are frontal 4,259 = **4,069 bilateral + 190 unilateral**, lateral 1,651, sunrise 161, over 3,706 patients. The difference is the 19 images the section-13 gates dropped between manifest and shard: 6,090 − 6,071 = 19 = 17 + 2. **State the pipeline stage whenever either number is used.** Three cohort patients have no written crop at all (2 train, 1 test), which is why the test split is 741 patients but 740 patients with crops.
- **Laterality-assertion violations: 0, train and val** (images whose manifest `laterality` did not equal `contra_side` on a single-knee view, or whose side could not be resolved). These were routed to the failure report and NOT processed. **No equivalent figure was recorded for the test run.**
- **The test run additionally recorded 6 `inverted` disagreements with the DICOM tag (0.493% of 1,218 scheduled images).** The DICOM's own PhotometricInterpretation tag wins in every case and the manifest flag is reported only, so a wrong `inverted` flag cannot corrupt a crop. No equivalent count survives for the train+val run.

**WHY THIS DOCUMENT IS LOAD-BEARING, and must not be regenerated.** `derived-data/cohort/preprocess_run.json` was **overwritten by the 2026-07-29 test run**: it and `preprocess_run_test.json` are byte-identical and both describe the test run only (1,218 scheduled, 1,216 written, 740 patients, 3 failures, 6 `inverted` disagreements), and it now points at a shard directory that no longer exists. **The machine-readable summary of the train + val preprocessing run is gone.** Its exclusion counts survive in exactly two places: the prose of this file (`excessive_masking` 17, `view_mask_drift` 1, `localization_failed` 0, fallback rate 0 of 4,855, laterality-assertion violations 0), and `outputs/tables/preprocess_failures.csv`, **which holds only the test run's 3 rows**. Any Methods sentence about preprocessing exclusions must be assembled from those two sources; it cannot be read out of the JSON, and re-running `src.crop_qa` or `src.preprocess_images` to "refresh" this file would destroy the only surviving record.

## 3. Protocol section 23 (i) — image-level manual QA: DECLINED (deviation D40)

**DECIDED 2026-08-12: this review will not be performed.** The author judged a 400-image, two-reviewer, six-item review — roughly 4,800 independent judgements before adjudication, and 8 to 11 person-hours of two qualified readers' time — **disproportionate** to what it could establish on this cohort, and declined it. **Deviation D40 is the entry of record** and states what the review would have measured and what its absence costs. It is a decision, not a deferral: no reviewer will be sought, no schedule exists, and **the sampling material has been withdrawn from the project** (see the removal list below). Everything under this heading is retained as the record of what was designed and drawn, not as a queue of pending work.

- Required by the protocol: **400** index images, **2** independent reviewers with orthopedic or MSK imaging experience, scored on **laterality, view, native_knee, crop_adequacy, burned_in_text, non_knee_content**, with raw agreement, Cohen kappa, third-reviewer adjudication and a 2% critical-error gate. **None of it was done and none of it will be.**
- Sampled (2026-08-10, before the decline): **400** images over **393 patients** (stratified on split x view x contra_side x bilateral/unilateral, proportional largest-remainder, seed 20250720, all 24 non-empty strata represented) from a pool of **6,071** finished crops covering all three splits.
- **Splits: requested `['train','val','test']`; present `['train','val','test']`. By split: train 273 / val 45 / test 82.** The sample was **rebuilt on 2026-08-10** so that it would cover the sealed split. The version generated on 2026-07-26 was train 344 / val 56 / **test 0**, because the 1,216 test crops were not written until 2026-07-29. The 318 train and val rows of the rebuilt sample were a strict subset of the original, so no completed reviewer work would have been discarded, and every `P####` index assignment was unchanged because the index is burned into the panel filenames.
  - **A historical trap, kept here because it must not be re-armed.** This line used to carry a bracketed "missing splits" clause naming the sealed split, and a manuscript limitation was keyed off that literal phrase. When the sample was rebuilt the phrase went away, the match stopped, and the limitation **silently downgraded to a weaker sentence**. **Do not reintroduce that token anywhere in this file, not even inside a quotation or an example**: `src.make_manuscript` still carries the regex as a registered historical pattern that `tests/test_make_manuscript.py` asserts matches **nothing**, so reintroducing it fails the suite. The workbook the detector later read is gone with the rest of the apparatus, and the module now reads this review's state from the **deviation register** instead — see the note under section 6.
- By view: `{'frontal': 275, 'lateral': 109, 'sunrise': 16}`; by split: `{'train': 273, 'val': 45, 'test': 82}`; by laterality kind: `{'bilateral_B': 258, 'unilateral': 142}`; by contralateral side: `{'L': 212, 'R': 188}`.
- The sample spanned **all** splits on purpose. It was LABEL/CROP quality assurance carrying pixels and acquisition metadata and **no outcome column at all**, so it was NOT outcome-unblinding and did not touch the sealed-test guarantee. It was, however, drawn after the sealed read rather than before it, which is what D22 and D31 record.
- **REMOVED on 2026-08-12, and recoverable from no path in this project:** the blank adjudication workbook `derived-data/cohort/image_audit_workbook.csv` (400 rows x 43 columns, every reviewer cell blank) and its `image_audit_workbook_v1_20260726.csv` archive; the reviewer panels in `derived-data/cohort/qa_panels/`; `derived-data/cohort/crop_qa_index_key.csv`; and the scoring application `src/qa_review_app.py`. **Two files under those paths were deliberately kept and must not be removed:** `src/crop_qa.py`, because `src/interpretability.py` imports `residual_marker_scan` from it and that function produced the 18.5% residual-marker rate this document reports in section 2; and the single panel `derived-data/cohort/qa_panels/20001294_frontal_f003764b5803.png`, because it is the source film for the manuscript's Figure 1 and the DICOMs are gone.

### The 82 test-split rows are crop-only, and one item is not assessable on them

The 584 panels rendered on 2026-07-26 carried the **full film plus the pre-mirror crop**. The source DICOMs are gone, so **that set could never have grown**. The 82 test-split panels added on 2026-08-10 were therefore **crop-only**: the left half of the panel was a plate reading "NO FULL FILM — half-select NOT visually verifiable", and the right half the finished 512 x 512 crop. The workbook carried a boolean column `panel_has_full_film`, **318 True (all train and val) / 82 False (all test)**.

**This subsection still matters after the decline, and it is the reason the decline costs less on one item than on the other five.**

- **Five of the six items were fully decidable from a finished crop** — `view`, `native_knee`, `crop_adequacy`, `burned_in_text`, `non_knee_content` — and were assessable on all three splits. **Those five are what the decline gives up**, on every split rather than only on the sealed one.
- **`laterality` was not decidable, by construction rather than by oversight.** Every pre-index film shows two native knees, `standardize_to_left` mirrors right knees away, and the border mask removes any burned-in L/R marker, so a finished crop is anatomically identical whether the correct half or the wrong half was taken. Asking a reviewer to confirm half-select from the crop is an undecidable question they would answer in good faith.
- **That was a permanent limitation before the decline, not a scheduling problem.** No amount of reviewer time recovered it, because the evidence no longer exists. **So declining this review removes no evidence about test-split half-selection that would otherwise have existed.**
- **What the manuscript must therefore say:** the 400-image review was declined and none of its six items was scored; half-selection on the reported split was never assessable, by construction. **It must never say that laterality was verified on 400, nor that the review is outstanding, planned or imminent.** The laterality evidence the study does hold is the section 7 audit in section 5 below.

**Scoring convention, retained as the design record.** Every `<item>_r<k>` cell was to take exactly one of `OK`, `ERROR` or `NOT_ASSESSABLE` (aliases: PASS/FAIL, Y/N, 1/0; and `N/A`, `UNASSESSABLE`, `NOT ASSESSABLE`, `CANNOT_ASSESS`, `UNDECIDABLE` for the third), with a blank cell meaning "not yet reviewed" and a bare `NA` parsing the same way. `NOT_ASSESSABLE` existed because of the 82 crop-only test rows above: without it a finished review would be indistinguishable from an abandoned one. It was excluded from the agreement, kappa and critical-error denominators and reported separately as `n_not_assessable`. `src/crop_qa.py` still implements this vocabulary, `cohens_kappa` and the 2% gate, and `tests/test_crop_qa.py` still tests them; **nothing invokes them, because there is no workbook to score and no review to score it for.**

**There is no longer a way to score this review, and that is deliberate.** The two-reviewer scoring application `src/qa_review_app.py` was removed on 2026-08-12 with the workbooks and the panels, and its five tests were removed with it. Do not rebuild any of it in order to "complete" a review the author has declined; if that decision is ever reversed, the reversal is a new deviation entry and the material is re-drawn from `src/crop_qa.py` under a fresh seed record, not recovered from here.

## 4. Protocol section 23 (ii) — outcome-record audit

**SAMPLED AND UNADJUDICATED. This is the one review of the three that is still outstanding**, and it is the sole reason deviation D22 is PARTIAL rather than closed. It was drawn as designed and nothing has been scored.

- Required: **200** outcome records reviewed via CPT chronology and available post-event images. Sampled: **200**.
- Composition: **100 events** and **100 non-events** drawn from 3,709 cohort patients carrying 533 events. Events are deliberately over-sampled — the endpoint being validated is the event, and a sample at the natural prevalence would give the reviewer too few to check.
- side_source: `{'coded': 120, 'recovered': 80}`; **split: `{'train': 144, 'val': 15, 'test': 41}`.**
- 200 of the sampled records carry at least one knee-arthroplasty CPT row (556 rows in total).
- **REMOVED on 2026-08-12 with the rest of the reviewer apparatus:** the workbook `derived-data/cohort/outcome_audit_workbook.csv`, the long-format chronology `derived-data/cohort/outcome_audit_cpt_rows.csv`, and the aggregate summary `outputs/tables/outcome_audit_summary.csv`. **No path in this project holds this sample any longer**, so nothing may be written that reads one, and re-drawing it means re-running `src/crop_qa.py` and recording a fresh seed rather than recovering the old draw. The composition figures above are the surviving record of what was drawn.
- The reviewer was to fill `reviewer_event_confirmed_Y_N`, `reviewer_event_date`, `reviewer_event_side`, `reviewer_agrees_with_primary_event_Y_N` and `reviewer_notes`. **None of those cells was ever filled**, so there is no adjudication, no agreement statistic and no kappa for this review either.
- The sample spans all splits because it validates the LABEL-EXTRACTION algorithm, not the model. Adjudication results must not be used to select, tune or threshold any model; a discrepancy re-opens the cohort lock and the labels are rebuilt for every patient, which is the only legitimate response.

## 5. Laterality QA audit (protocol section 7) — DONE

**ADJUDICATED 2026-08-12. The deterministic index-side recovery rule agreed with manual adjudication on 196 of 200 sampled development patients: 98.0%, 95% CI 95.0 to 99.2 (Wilson), a 2.0% observed error rate.** The register entry of record is the 2026-08-12 update inside **D22**, which carries the machine-read form of that clause and the do-not-reword note beside it. **Quote the Wilson interval and name the method.** Four intervals were computed before one was chosen — Wilson 95.0 to 99.2, Agresti-Coull 94.8 to 99.4, Clopper-Pearson 95.0 to 99.5, normal approximation 96.1 to 99.9 — and the last is wrong this close to the boundary and must never appear anywhere.

**Provenance, and it is the reason nothing under this heading points at a scored file.** The adjudication was performed **on paper**. The per-patient reviewer index-side and agreement records are held offline and will not be digitised, the sampling workbook is not kept in the repository, and no audit summary table is published. **The figure is author-attested rather than recomputable** from anything in this project or its archive. That is a deliberate choice: do not read the absence of a scored file as an oversight, and do not "fix" it by regenerating one.

**Section 7's own PASS rule was not satisfied, and the disclosure replaces the gate.** The last bullet of this section states that the audit passes only on a perfect match across every row; there are **four disagreements**, so this audit is **completed and reported, not passed**. The author does not re-open the cohort lock on those four: no model, split, label or estimate is rebuilt, and the sealed read stands. The 2.0% observed error rate is what the study reports in place of a pass, and no document may describe section 7 as passed or as clearing a gate.

**Train and val only, and it stayed that way.**

- Patient sample: **200** (minimum required 200). **Splits: train 168 / val 32 / test 0.**
- **Result: 196 of 200 agreed, 4 disagreed.** 98.0% agreement, 95% CI 95.0 to 99.2 (Wilson), 2.0% observed error rate.
- side_source composition: `{'recovered': 133, 'coded': 67}`.
- The sample **deliberately over-samples `side_source == "recovered"`** patients (1,828 of 3,709 in the cohort). Those patients' index laterality was INFERRED from concordant signals rather than coded on the CPT modifier, so they carry all of the residual side-assignment risk; the coded patients are included only as a control.
- `index_side` **cannot be judged from a contralateral crop** — the crop is by construction the other knee. Each row therefore also carried a full-film panel, which is what made the question answerable.
- **This sample was NOT extended to the test split, and could not have been.** Test patients cannot have a full-film panel, so every added row would have to be answered "cannot tell", which is not a validation. **The consequence for the paper is stated rather than smoothed over: the 98.0% figure validates the recovery rule on development patients and does not directly verify the 371 test patients whose index side was recovered by rule.** Section 7 validates a patient-level rule applied identically across splits, and split assignment is random with respect to laterality, so the development sample is an unbiased estimate of the cohort-wide error rate including the test patients' — but it is an estimate, not a direct check of the reported split.
- **One gap worth recording, and it qualifies the 98.0%.** Section 7 asks the reviewer to judge the index side "using CPT chronology **and** available radiographic evidence", but this sample carried the algorithm's own signals (`side_source`, `n_concordant_signals`, `tier_name`) alongside the full film rather than the raw CPT chronology the section 23 (ii) sample carries. The adjudication was therefore made against the film plus a summary of the rule rather than against the underlying billing records, which makes it a partly rule-informed check rather than a fully independent one. Attaching `cpt_chronology` would have fixed it; the code to build it exists in `src/crop_qa.py`.
- **REMOVED on 2026-08-12:** the patient-level file `derived-data/cohort/laterality_audit_sample.csv`, the crops in `derived-data/cohort/laterality_audit_crops/`, and the aggregate summary `outputs/tables/laterality_audit_summary.csv`. **No path in this project holds this sample or its adjudication.** The mean of 1.7 concordant signals per patient, recorded in the removed summary, survives only in this sentence.
- The reviewer filled `reviewer_index_side`, `reviewer_agrees_Y_N` and `reviewer_notes` per row **on paper**. The audit PASSES only if the reviewed index side matches the coded/recovered `index_side` for every row; any disagreement re-opens the cohort lock. **Four rows disagreed and the lock was not re-opened**, which is the departure recorded three paragraphs above and in D22.

## 6. Sign-off

Signing means: I inspected the contact sheet, the outlined half of each full film is the contralateral knee, no index-knee pixels or laterality markers survive in the crops, and the protocol section 23 audits above are adjudicated and passed.

| field | value |
|---|---|
| Reviewer name | |
| Role | |
| Date (YYYY-MM-DD) | |
| Signature | |
| Result (PASS / FAIL) | |
| If FAIL, defect and required fix | |

### The state of each review — settled 2026-08-12

**This block is deliberately empty and must stay empty permanently.** Signing it asserts, in the sentence above, that the section 23 audits "are adjudicated and passed"; one of them is declined and the other is unadjudicated, so no signature can be truthful. This is no longer a to-do list.

| # | review | state |
|---|---|---|
| 1 | Section 7 laterality audit: 200 patients adjudicated | **DONE 2026-08-12.** 196 of 200 agreed, 98.0%, 95% CI 95.0 to 99.2 (Wilson), 2.0% observed error rate. Adjudicated on paper; author-attested, not recomputable. Section 7's own all-rows PASS rule is **not** satisfied: four disagreements, and the cohort lock was not re-opened. |
| 2 | Section 23(i): two reviewers score all 400 images x 6 items | **DECLINED 2026-08-12 (D40).** Not deferred and not scheduled. Nothing was scored, no reviewer was secured, and the sample, the panels and the scoring application were removed. |
| 3 | Section 23(i): per-item raw agreement, Cohen kappa, and the 2% critical-error gate | **never measured, and never will be.** The gate was not failed; it was not tested. Do not report it as passed. |
| 4 | Section 23(ii) outcome audit: 200 records scored and adjudicated | **OUTSTANDING. 0% scored.** The only item still open, and the sole reason D22 is PARTIAL rather than closed. Its workbook was removed with the rest, so re-drawing it means re-running `src/crop_qa.py`. |

**How the manuscript reads this file now.** `src.make_manuscript.manual_review_clause` no longer reads a workbook, because there is none. It reads the section 7 result and the section 23 decline out of **`outputs/protocol_deviations.md`**, through the registered patterns in `make_manuscript.SCRAPED_PATTERNS`, and it still counts the four unticked boxes in section 1 and scrapes the bilateral-film and residual-marker clauses out of this document. `tests/test_make_manuscript.py::test_every_scraped_pattern_still_matches_its_source` fails loudly if any of those stops matching. **The state of the reviews is therefore edited in the register, not here**; this file describes the material, and the register decides the sentence.

**No agent could have discharged the declined review.** Protocol section 23 requires two *independent* human reviewers and the quantity being reported *is* their agreement, so a generated score would not be a weak substitute for the measurement — it would be fabrication of the primary evidence. That is why the review was either done by humans or declined, and it was declined.

**Consequences, all of which belong in the manuscript's limitations and not only here:** no human has inspected any image the reported results rest on, because the review that would have was declined; no agreement statistic or Cohen kappa exists for either protocol section 23 review; the D2 recovered-index-side cohort, 49.3% of the primary cohort, is validated **indirectly** by the section 7 audit at 98.0% on development patients and not directly on the 371 test patients recovered by rule; and the section-23(i) laterality item could never have been assessed on the test split in any case, because the full films are gone. Deviation **D22** is the entry of record and is **PARTIAL**; **D40** records the decline. The original closing line of this file — "Until `Result` reads PASS, `notebooks/train_colab.ipynb` must not be run" — is retained here only as the historical intent: that notebook was superseded, training and the sealed read went ahead without this signature, and **D31** records that decision.
